# Core Framework
Django==5.0.3
djangorestframework==3.15.1   # REST APIs for vitals, location

# Real-time Communication
channels==4.0.0               # WebSockets support in Django
channels-redis==4.2.0         # Use Redis for pub/sub (if you want scalability)

# Data Handling
paho-mqtt==2.1.0              # For MQTT communication with ambulance devices
requests==2.32.3              # For calling external APIs (like QoD/Location APIs)

# Database
psycopg2-binary==2.9.9        # If using PostgreSQL (optional, remove if using SQLite/MySQL)
dj-database-url==2.2.0        # Easy DB config from environment

# CORS (React ↔ Django communication)
django-cors-headers==4.3.1

# Environment Management
python-dotenv==1.0.1

# (Optional but useful)
gunicorn==21.2.0              # For deployment
whitenoise==6.6.0             # Serve static files in production
